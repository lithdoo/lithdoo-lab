import fs from 'fs'; import os from 'os'; import path from 'path';
import { loadPlayPrompt, type PlayPromptName } from './prompts'; import type { PlayAiSession } from './types';
function config():string{return '[[llm_api]]\nname = "deepseek"\nmodel = "deepseek-chat"\nbase_url = "https://api.deepseek.com/v1"\napi_key_env = "DEEPSEEK_API_KEY"\n\n[promptpile]\nllm_api = "deepseek"\ndir = "./messages"\ntools_file = "./readonly.tools.toml"\nquiet = true\n';}
export function createPlayAiSession(prompt:PlayPromptName,userContent:string,toolsSource:string):PlayAiSession{const root=fs.mkdtempSync(path.join(os.tmpdir(),'day-loom-play-'));const messagesDir=path.join(root,'messages');fs.mkdirSync(messagesDir,{recursive:true});const session={root,messagesDir,toolsFile:path.join(root,'readonly.tools.toml'),promptpileConfig:path.join(root,'promptpile.toml'),draftFile:path.join(root,'unused.json'),playerContextRoot:path.join(root,'unused-context')};fs.writeFileSync(session.promptpileConfig,config(),'utf8');fs.copyFileSync(toolsSource,session.toolsFile);fs.writeFileSync(path.join(messagesDir,'[0]system.md'),loadPlayPrompt(prompt),'utf8');fs.writeFileSync(path.join(messagesDir,'[1]user.md'),userContent,'utf8');return session;}
export function cleanupPlayAiSession(session:PlayAiSession):void{fs.rmSync(session.root,{recursive:true,force:true});}
