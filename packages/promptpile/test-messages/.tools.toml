# Minimal tools file for local runs (explicit --tools-file or TOOLS_FILE required).
[[tools]]
name = "promptpile_test_placeholder"
description = "Replace with real tools for your project"
parameters = { type = "object", properties = {} }
